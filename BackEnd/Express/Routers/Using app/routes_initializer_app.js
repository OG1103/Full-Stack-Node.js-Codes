import declareRoutes from "./exampleAppInstance";
// Function to initialize routes using direct app instance methods
const initializeRoutes = (app) => {
   declareRoutes(app);
};

export default initializeRoutes;
