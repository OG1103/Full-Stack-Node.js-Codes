// This is a default function that accepts the Express 'app' instance as a parameter.
// The purpose of this function is to define routes or import them from other files
// and then register those routes with the app instance.
export default(app)=>{
    // define routes
    // or import routes from seperate files and declare them either by using app.use and using router function in the other file
        // or the routs are declared in the sperate file under a default function that takes app and i call that function here passing the app parameter
}