
// Example: Type Aliases

// Alias for a primitive type
type UserID = string;
let id: UserID = "12345";

// Alias for an object type
type Point = {
    x: number;
    y: number;
};

const point: Point = { x: 10, y: 20 };
